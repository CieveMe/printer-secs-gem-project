# 2026-06-05 正式交付归档

## 一句话结论

当前项目已经达到“可交付给现场继续联调”的状态：同一个 `PrinterSecsGem.Eq.exe` 进程里同时运行状态界面、SECS/GEM 后台连接、RFID 串口读写、Zebra USB 打印。Host 指令和界面按钮共用同一个硬件通道，不再需要 UI 模式和 SECS 模式二选一。

## 项目路径

```text
D:\Desktop\打印机项目\printer-secs-gem-project
```

## 当前交付方式

首次部署给完整文件夹，文件夹内包含：

```text
PrinterSecsGem.Eq.exe
App.config
log4net.config
zebra-command-line/
secs-simulator/
CONFIG_GUIDE_CN.txt
stop-printer-secs-gem.cmd
```

后续普通功能更新只替换：

```text
PrinterSecsGem.Eq.exe
```

原因：当前发布方式是框架依赖 single-file exe。程序自身逻辑和主要依赖已经打进 `PrinterSecsGem.Eq.exe`，而现场配置、日志配置、Zebra SDK 保持外置。

## 最新产物

完整部署包：

```text
D:\Desktop\打印机项目\printer-secs-gem-project\publish\printer-secs-gem-folder-realprint-ui-secs-20260605.zip
```

只替换 exe 的更新包：

```text
D:\Desktop\打印机项目\printer-secs-gem-project\publish\printer-secs-gem-exe-update-ui-secs-statusfix-20260605.zip
```

单独给现场用的两个辅助文件：

```text
D:\Desktop\打印机项目\printer-secs-gem-project\publish\printer-secs-gem-folder-realprint-ui-secs-20260605\CONFIG_GUIDE_CN.txt
D:\Desktop\打印机项目\printer-secs-gem-project\publish\printer-secs-gem-folder-realprint-ui-secs-20260605\stop-printer-secs-gem.cmd
```

## 当前正式运行方式

现场正式运行只需要双击：

```text
PrinterSecsGem.Eq.exe
```

双击后程序会：

1. 打开 WinForms 状态界面。
2. 后台启动 SECS/GEM EQ worker。
3. 根据 `App.config` 主动连接 Host。
4. 自动打开 RFID 串口。
5. 接收 Host 下发的 SECS 指令。
6. 通过同一个串口实例操作 RFID 读写器。
7. 通过 Zebra SDK USB Direct 发送 ZPL 给 Zebra 打印机。

备用纯后台模式仍保留：

```text
run.cmd
```

但正式现场优先使用双击 exe 的 GUI 模式。

## 当前技术栈

- .NET 8 / WinForms
- secs4net
- log4net
- System.IO.Ports
- Zebra SDK command_line

目标电脑需要安装：

```text
.NET 8 Desktop Runtime x64
```

Zebra SDK 不需要现场单独安装，因为首次完整部署文件夹里已经包含：

```text
zebra-command-line/
```

## 当前关键配置

配置文件：

```text
App.config
```

核心默认值：

```xml
<add key="secs4net:IpAddress" value="127.0.0.1" />
<add key="secs4net:Port" value="5000" />
<add key="secs4net:DeviceId" value="1" />
<add key="secs4net:IsActive" value="true" />

<add key="ERackHardware:Enabled" value="true" />
<add key="ERackHardware:PortName" value="COM11" />
<add key="ERackHardware:BaudRate" value="57600" />
<add key="ERackHardware:DeviceAddress" value="1" />
<add key="ERackHardware:InventoryMode" value="4" />
<add key="ERackHardware:KeepPortOpen" value="true" />
<add key="ERackHardware:WriteTagStartPage" value="1" />
<add key="ERackHardware:WriteTagWaitCount" value="20" />

<add key="Printer:RealPrintEnabled" value="true" />
<add key="Printer:Mode" value="ZebraCommandLine" />
<add key="Printer:ZebraCommandLineAssembly" value="zebra-command-line/SdkApi.Desktop.CommandLine.dll" />
<add key="Printer:ZebraConnectionType" value="Usb" />
<add key="Printer:ZebraPrinterAddress" value="" />
```

说明：

- `secs4net:IsActive=true`：本程序作为 EQ 主动连接 Host。
- `ERackHardware:PortName=COM11`：当前现场实测 COM11 可用。
- `ERackHardware:WriteTagStartPage=1`：现场已验证写 tag 成功的起始页。
- `Printer:RealPrintEnabled=true`：真实发送到 Zebra 打印机。
- `Printer:ZebraPrinterAddress` 为空：自动发现 USB Zebra 打印机。

## 当前支持的 SECS 指令

```text
S1F1  -> S1F2    连通性
S1F3  -> S1F4    设备状态
S8F3  -> S8F4    打印
S10F11 -> S10F12 写 RFID tag
S5F11 -> S5F12   读 RFID / 查询货架状态
```

已有但还未做成常驻主动触发：

```text
S6F11 tag 读取事件上报报文工厂
```

## 已验证的真实联调结果

### 1. SECS 连接

GUI 日志显示：

```text
SECS state: Connecting
SECS state: Connected
SECS state: Selected
```

命令日志显示：

```text
Sent Control Message: SelectRequest
Receive Control message: SelectResponse
HSMS connection state: Selected
```

结论：HSMS 连接和 Select 成功。

### 2. S1F1 / S1F3

GUI 日志显示：

```text
Received Host message S1F1.
Reply S1F2 prepared for Host.
Received Host message S1F3.
Reply S1F4 prepared for Host.
```

结论：基础 SECS 收发成功。

### 3. RFID 读

Host 发：

```text
S5F11
```

GUI 日志显示：

```text
S5F11 shelf status result: code=0, tag=EFS08IAA556677881122334455667788, loaded=True.
Reply S5F12 prepared for Host.
```

后续写入后再读：

```text
S5F11 shelf status result: code=0, tag=EFS08IZS556677881122334455667788, loaded=True.
```

结论：Host 通过 SECS 触发真实 RFID 读取成功，读回 tag 并返回 S5F12。

### 4. RFID 写

Host 发：

```text
S10F11
tag=EFS08IZS
```

GUI 日志显示：

```text
S10F11 write tag command: location=LOC001, tag=EFS08IZS.
S10F11 write tag result: code=0, description=tag written.
Reply S10F12 prepared for Host.
```

命令日志显示：

```text
Sending ERack RFID write request: port=COM11, baudRate=57600, address=1, startPage=1, tag=EFS08IZS, hardwareTag=SZI80SFE
ERack RFID write completed
```

结论：Host 通过 SECS 触发真实 RFID 写入成功，返回 S10F12。

### 5. 打印

Host 发：

```text
S8F3
content=EFS08IZS
copies=1
```

GUI 日志显示：

```text
S8F3 print command: content=EFS08IZS, copies=1.
S8F3 print result: code=0, description=zpl sent to zebra printer: \\?\usb#vid_0a5f&pid_0168#...
Reply S8F4 prepared for Host.
```

命令日志显示：

```text
Sent ZPL through Zebra SDK: printer=\\?\usb#vid_0a5f&pid_0168#...
Print command result: success=True, code=0
```

结论：Host 通过 SECS 触发 Zebra USB Direct 打印成功，返回 S8F4。

### 6. 状态界面同步

最新状态界面已正确显示：

```text
SECS: HSMS Selected
COM: COM11 open
RFID: EFS08IZS556677881122334455667788
Last Print: Printed: EFS08IZS
```

结论：GUI 上方状态栏和下方日志均能反映最新 SECS 指令处理结果。

## RFID 短写说明

写 tag 内容长度必须是：

```text
8 / 16 / 24 / 32 个 ASCII 字符
```

当前按约定：短写 8、16、24 字符时，只写对应长度，不补空格清除旧数据。

因此读回 32 字节时，后面可能仍然显示旧内容，例如：

```text
EFS08IZS556677881122334455667788
```

这不是错误，是当前确认后的行为。

## SEComSimulator 辅助文件

新版 SMD：

```text
samples\secs\PrinterSecsGem-UI-SECS-Test.SMD
```

已复制到：

```text
D:\Desktop\打印机项目\SECS_Simulator\PrinterSecsGem-UI-SECS-Test.SMD
publish\printer-secs-gem-folder-realprint-ui-secs-20260605\secs-simulator\PrinterSecsGem-UI-SECS-Test.SMD
```

一键打开模拟器脚本：

```text
scripts\open-secs-simulator.cmd
publish\printer-secs-gem-folder-realprint-ui-secs-20260605\secs-simulator\open-secs-simulator.cmd
```

模拟器建议配置：

```text
Role: Host
HSMS: Passive / Listen
IP: 127.0.0.1
Port: 5000
Device ID: 1
Protocol Type: HSMS
```

## 辅助说明和脚本

配置说明文件：

```text
CONFIG_GUIDE_CN.txt
```

用途：

- 说明如何改 Host、COM、打印开关。
- 说明更新 exe 的方式。
- 说明常见问题排查。

一键结束进程脚本：

```text
stop-printer-secs-gem.cmd
```

脚本执行：

```bat
taskkill /F /IM PrinterSecsGem.Eq.exe
```

用途：

- 当覆盖 exe 提示文件被占用时，双击该脚本结束旧程序。
- 结束后再覆盖新的 `PrinterSecsGem.Eq.exe`。

## 重要源码文件

```text
src\PrinterSecsGem.Eq\Program.cs
src\PrinterSecsGem.Eq\StatusUi\StatusDashboardForm.cs
src\PrinterSecsGem.Eq\StatusUi\StatusUiEventBus.cs
src\PrinterSecsGem.Eq\Secs\SecsPrimaryMessageWorker.cs
src\PrinterSecsGem.Eq\Secs\SecsMessageDispatcher.cs
src\PrinterSecsGem.Eq\Hardware\ERack\ERackSerialHardwareGateway.cs
src\PrinterSecsGem.Eq\Hardware\ERack\ERackTagDecoder.cs
src\PrinterSecsGem.Eq\Printing\ZebraCommandLinePrinterGateway.cs
```

## 当前可以对外表达的状态

可以说：

```text
程序主链路已经跑通：Host 通过 SECS 连接 EQ，能下发读 RFID、写 RFID、打印指令，EQ 能调用现场串口硬件和 USB Zebra 打印机，并返回对应 SECS 回复。界面也能同步显示 SECS 状态、COM 状态、RFID 当前值和最近打印结果。
```

不建议说得过满：

```text
所有现场异常都完全解决。
```

因为后续仍可能遇到：

- 现场 Host 正式 CEID/RPTID/VID 编号确认。
- 现场真实 Host 消息格式调整。
- 多台 Zebra 打印机时需要固定打印机地址。
- RFID 读写器 COM 口/线材/驱动稳定性问题。
- 打印版式根据正式标签纸继续微调。

## 下个会话优先事项

1. 先读取 `PROJECT_CONTEXT.md` 和本归档文件。
2. 如果继续开发，先确认是否只需要改 exe。
3. 若只是交付沟通，使用本归档中的“当前可以对外表达的状态”。
4. 如果现场新反馈问题，优先看：
   - `logs/printer-secs-gem.log`
   - GUI 日志
   - `App.config`
   - COM 是否仍是 COM11
   - Host 是否仍是 `127.0.0.1:5000 / DeviceId=1`

