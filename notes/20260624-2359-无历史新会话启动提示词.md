请接手这个项目，用中文回答，先读文件再行动，不要凭空猜。

项目路径：
`D:\Desktop\打印机项目\printer-secs-gem-project`

请先读取：

1. `D:\Desktop\打印机项目\printer-secs-gem-project\PROJECT_CONTEXT.md`
2. `D:\Desktop\打印机项目\printer-secs-gem-project\notes\20260624-2359-新会话交接-现场状态与GUI返工归档.md`
3. `D:\Desktop\打印机项目\printer-secs-gem-project\notes\20260624-二期远程桌面联调完整归档.md`
4. `D:\Desktop\打印机项目\printer-secs-gem-project\notes\20260621-项目完整归档-二期准备.md`

读完后先执行：

```powershell
git status --short
git diff --stat
```

当前重点：

GUI 仍然不可用，不要继续微调当前小格子布局。请直接重做状态界面。优先方案：

- `TabControl`：`状态` / `操作` / `日志`
- `状态` 页用 `DataGridView` 或 `ListView` 两列显示：`项目` / `状态`
- 服务端和客户端状态分开显示
- 长路径、长路由事件、异常详情放日志页或详情框
- 1024x768 必须截图验证

现场最新状态：

- 真实设备已接好，COM11 已打开。
- `Runtime:Mode=Both`
- SECS 被动监听 `0.0.0.0:5000`
- ERACK Server 监听 `127.0.0.1:7801`
- Unit 已注册：`UNIT001` / `SHELF001`
- Routes：`1 online: SHELF001`
- 仿真禁用，真实 Zebra 打印开启。

注意事项：

- 工作区很脏，有大量二期未提交改动和未跟踪文件，不要 `revert`、不要清理。
- 普通最小更新包不能直接覆盖现场 `App.config`。
- 如果需要修复现场 `App.config` 编码，只能通过 `run-after-copy-repair-app-config.cmd`，让脚本先备份旧配置，再用 `App.config.reference` 重建并迁移同名 key。
- 最小包单纯覆盖不会改变现场 `App.config` 编码，因为最小包不包含 `App.config`。
- 旧 SMD 里有 `SHELF999` 负向测试，现场正常测试只用 `secs-simulator\PrinterSecsGem-UI-SECS-Test.SMD`。
- S6F11/S8F4 中文 Description 仍需确认编码；如果 ASCII `A(...)` 显示 `????`，下一轮要验证 `J(...)` 或改为 ASCII 枚举。
